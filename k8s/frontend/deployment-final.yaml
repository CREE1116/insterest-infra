apiVersion: apps/v1
kind: Deployment
metadata:
  name: feed-test-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: feed-test-frontend
  template:
    metadata:
      labels:
        app: feed-test-frontend
    spec:
      containers:
      - name: frontend
        image: nginx:alpine
        ports:
        - containerPort: 80
        # 컨테이너 시작 시 ConfigMap의 파일들을 /usr/share/nginx/html로 복사
        command: ["/bin/sh", "-c"]
        args:
          - |
            cp /patch/index.html /usr/share/nginx/html/index.html && \
            cp /patch/app.js /usr/share/nginx/html/app.js && \
            cp /patch/style.css /usr/share/nginx/html/style.css && \
            nginx -g 'daemon off;'
        volumeMounts:
        - name: patch-vol
          mountPath: /patch
      volumes:
      - name: patch-vol
        configMap:
          name: feed-test-frontend-html
---
apiVersion: v1
kind: Service
metadata:
  name: feed-test-frontend
spec:
  selector:
    app: feed-test-frontend
  ports:
  - port: 80
    targetPort: 80
