apiVersion: v1
kind: Service
metadata:
  name: console-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: console-frontend
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: console-frontend
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: console-frontend
  template:
    metadata:
      labels:
        app: console-frontend
    spec:
      containers:
      - name: console-frontend
        image: cree1116/console-frontend:c59be4139ff89cac6200ad6919219cc11a2e0307
        imagePullPolicy: Always
        ports:
        - containerPort: 80
